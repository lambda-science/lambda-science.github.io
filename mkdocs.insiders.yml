INHERIT: mkdocs.yml
plugins:
  - search
  - social
  - tags
  - blog:
      post_date_format: medium
      post_url_format: "{slug}"
      archive_url_format: "{date}"
      archive: false
      categories: false